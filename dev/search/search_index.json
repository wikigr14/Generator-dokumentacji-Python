{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Witaj w Projekcie 18","text":"<p>To jest g\u0142\u00f3wna strona naszej automatycznie generowanej i wersjonowanej dokumentacji.</p> <p>Skorzystaj z zak\u0142adek w menu powy\u017cej, aby przegl\u0105da\u0107 dokumentacj\u0119 wygenerowan\u0105 prosto z kodu \u017ar\u00f3d\u0142owego projekt\u00f3w napisanych w j\u0119zykach Python oraz Scala.</p>"},{"location":"#szybki-start","title":"Szybki start","text":"<ul> <li>Modu\u0142 Pythona zosta\u0142 wygenerowany za pomoc\u0105 wtyczki <code>mkdocstrings</code>.</li> <li>Modu\u0142 Scali to w pe\u0142ni zintegrowany natywny format <code>Scaladoc</code>.</li> <li>Diagramy architektoniczne s\u0105 generowane automatycznie z kodu \u017ar\u00f3d\u0142owego.</li> </ul>"},{"location":"architecture/","title":"Architektura systemu","text":"<p>Diagramy klas generowane automatycznie z kodu \u017ar\u00f3d\u0142owego w ramach potoku CI/CD.</p>"},{"location":"architecture/#wzorzec-strategii-modu-patnosci","title":"Wzorzec Strategii \u2014 modu\u0142 p\u0142atno\u015bci","text":"<p>Struktura wzorca Strategy zaimplementowanego w <code>src/python/pattern.py</code>:</p> <ul> <li><code>PaymentStrategy</code> \u2014 abstrakcyjna klasa bazowa definiuj\u0105ca kontrakt</li> <li><code>CreditCardPayment</code> \u2014 implementacja p\u0142atno\u015bci kart\u0105 kredytow\u0105</li> <li><code>PayPalPayment</code> \u2014 implementacja p\u0142atno\u015bci przez PayPal</li> </ul>"},{"location":"calculator/","title":"Dokumentacja API \u2014 Kalkulator","text":"<p>Automatycznie wygenerowana dokumentacja modu\u0142u <code>calculator.py</code>.</p>"},{"location":"calculator/#przykad-uzycia","title":"Przyk\u0142ad u\u017cycia","text":"BasicCalculatorScientificCalculatorMemoryCalculator <pre><code>from calculator import BasicCalculator\n\ncalc = BasicCalculator()\nprint(calc.add(10, 5))       # (1)\nprint(calc.divide(10, 3))    # (2)\nprint(calc.get_history())    # (3)\n</code></pre> <ol> <li>Zwraca <code>15.0</code></li> <li>Zwraca <code>3.3333...</code> \u2014 wynik jest typu <code>float</code></li> <li>Zwraca list\u0119 wszystkich operacji: <code>['10 + 5 = 15.0', '10.0 / 3.0 = 3.333...']</code></li> </ol> <pre><code>from calculator import ScientificCalculator\n\ncalc = ScientificCalculator()\nprint(calc.calculate(2, 10, \"power\"))  # (1)\nprint(calc.sqrt(144))                  # (2)\nprint(calc.sin(90))                    # (3)\n</code></pre> <ol> <li>Zwraca <code>1024.0</code> \u2014 pot\u0119gowanie: 2^10</li> <li>Zwraca <code>12.0</code></li> <li>Zwraca <code>1.0</code> \u2014 k\u0105t podany w stopniach, nie radianach</li> </ol> <pre><code>from calculator import MemoryCalculator\n\ncalc = MemoryCalculator()\ncalc.memory_store(100.0)              # (1)\ncalc.memory_add(calc.add(20, 5))      # (2)\nprint(calc.memory_recall())           # (3)\n</code></pre> <ol> <li>Zapisuje <code>100.0</code> do pami\u0119ci (odpowiednik klawisza MS)</li> <li>Dodaje wynik <code>20 + 5 = 25</code> do pami\u0119ci \u2014 pami\u0119\u0107 wynosi teraz <code>125.0</code></li> <li>Zwraca <code>125.0</code> \u2014 odpowiednik klawisza MR</li> </ol>"},{"location":"calculator/#dokumentacja-klas","title":"Dokumentacja klas","text":"<p>               Bases: <code>ABC</code></p> <p>Abstrakcyjna klasa bazowa kalkulatora.</p> <p>Definiuje wsp\u00f3lny interfejs dla wszystkich typ\u00f3w kalkulator\u00f3w. Przechowuje histori\u0119 wykonanych operacji.</p> <p>Attributes:</p> Name Type Description <code>_history</code> <code>list[str]</code> <p>Lista zapisanych operacji w formacie czytelnym dla cz\u0142owieka.</p> Source code in <code>src/python/calculator.py</code> <pre><code>class Calculator(ABC):\n    \"\"\"\n    Abstrakcyjna klasa bazowa kalkulatora.\n\n    Definiuje wsp\u00f3lny interfejs dla wszystkich typ\u00f3w kalkulator\u00f3w.\n    Przechowuje histori\u0119 wykonanych operacji.\n\n    Attributes:\n        _history (list[str]): Lista zapisanych operacji w formacie czytelnym dla cz\u0142owieka.\n    \"\"\"\n\n    def __init__(self) -&gt; None:\n        self._history: list[str] = []\n\n    @abstractmethod\n    def calculate(self, a: float, b: float, operation: str) -&gt; float:\n        \"\"\"\n        Wykonuje operacj\u0119 matematyczn\u0105 na dw\u00f3ch liczbach.\n\n        Args:\n            a (float): Pierwsza liczba (lewy operand).\n            b (float): Druga liczba (prawy operand).\n            operation (str): Nazwa operacji do wykonania.\n\n        Returns:\n            float: Wynik operacji.\n\n        Raises:\n            ValueError: Je\u015bli operacja nie jest obs\u0142ugiwana przez dany kalkulator.\n        \"\"\"\n\n    def get_history(self) -&gt; list[str]:\n        \"\"\"\n        Zwraca histori\u0119 wykonanych operacji.\n\n        Returns:\n            list[str]: Kopia listy historii operacji.\n        \"\"\"\n        return list(self._history)\n\n    def clear_history(self) -&gt; None:\n        \"\"\"Czy\u015bci histori\u0119 operacji.\"\"\"\n        self._history.clear()\n\n    def _record(self, entry: str) -&gt; None:\n        \"\"\"Zapisuje wpis do historii operacji.\"\"\"\n        self._history.append(entry)\n</code></pre> <p>               Bases: <code>Calculator</code></p> <p>Kalkulator podstawowy \u2014 cztery dzia\u0142ania arytmetyczne.</p> <p>Dziedziczy po klasie <code>Calculator</code>. Obs\u0142uguje operacje: <code>add</code>, <code>subtract</code>, <code>multiply</code>, <code>divide</code>.</p> Example <p>calc = BasicCalculator() calc.calculate(10, 3, \"divide\") 3.3333333333333335 calc.get_history() ['10.0 / 3.0 = 3.3333333333333335']</p> Source code in <code>src/python/calculator.py</code> <pre><code>class BasicCalculator(Calculator):\n    \"\"\"\n    Kalkulator podstawowy \u2014 cztery dzia\u0142ania arytmetyczne.\n\n    Dziedziczy po klasie `Calculator`. Obs\u0142uguje operacje:\n    ``add``, ``subtract``, ``multiply``, ``divide``.\n\n    Example:\n        &gt;&gt;&gt; calc = BasicCalculator()\n        &gt;&gt;&gt; calc.calculate(10, 3, \"divide\")\n        3.3333333333333335\n        &gt;&gt;&gt; calc.get_history()\n        ['10.0 / 3.0 = 3.3333333333333335']\n    \"\"\"\n\n    SUPPORTED_OPERATIONS = (\"add\", \"subtract\", \"multiply\", \"divide\")\n\n    def calculate(self, a: float, b: float, operation: str) -&gt; float:\n        \"\"\"\n        Wykonuje podstawow\u0105 operacj\u0119 arytmetyczn\u0105.\n\n        Args:\n            a (float): Pierwsza liczba.\n            b (float): Druga liczba.\n            operation (str): Jedna z: ``add``, ``subtract``, ``multiply``, ``divide``.\n\n        Returns:\n            float: Wynik operacji.\n\n        Raises:\n            ValueError: Je\u015bli operacja nie jest obs\u0142ugiwana.\n            ZeroDivisionError: Je\u015bli ``b == 0`` przy operacji ``divide``.\n        \"\"\"\n        if operation == \"add\":\n            result = a + b\n            self._record(f\"{a} + {b} = {result}\")\n        elif operation == \"subtract\":\n            result = a - b\n            self._record(f\"{a} - {b} = {result}\")\n        elif operation == \"multiply\":\n            result = a * b\n            self._record(f\"{a} * {b} = {result}\")\n        elif operation == \"divide\":\n            if b == 0:\n                raise ZeroDivisionError(\"Dzielenie przez zero jest niedozwolone.\")\n            result = a / b\n            self._record(f\"{a} / {b} = {result}\")\n        else:\n            raise ValueError(\n                f\"Nieobs\u0142ugiwana operacja: '{operation}'. \"\n                f\"Dost\u0119pne: {self.SUPPORTED_OPERATIONS}\"\n            )\n        return result\n\n    def add(self, a: float, b: float) -&gt; float:\n        \"\"\"Dodaje dwie liczby. Skr\u00f3t dla ``calculate(a, b, 'add')``.\"\"\"\n        return self.calculate(a, b, \"add\")\n\n    def subtract(self, a: float, b: float) -&gt; float:\n        \"\"\"Odejmuje ``b`` od ``a``. Skr\u00f3t dla ``calculate(a, b, 'subtract')``.\"\"\"\n        return self.calculate(a, b, \"subtract\")\n\n    def multiply(self, a: float, b: float) -&gt; float:\n        \"\"\"Mno\u017cy dwie liczby. Skr\u00f3t dla ``calculate(a, b, 'multiply')``.\"\"\"\n        return self.calculate(a, b, \"multiply\")\n\n    def divide(self, a: float, b: float) -&gt; float:\n        \"\"\"\n        Dzieli ``a`` przez ``b``. Skr\u00f3t dla ``calculate(a, b, 'divide')``.\n\n        Raises:\n            ZeroDivisionError: Je\u015bli ``b == 0``.\n        \"\"\"\n        return self.calculate(a, b, \"divide\")\n</code></pre> Dzielenie przez zero <p>Metoda <code>divide()</code> rzuca <code>ZeroDivisionError</code> gdy drugi argument wynosi <code>0</code>. Zawsze waliduj dane wej\u015bciowe przed wywo\u0142aniem.</p> <p>               Bases: <code>BasicCalculator</code></p> <p>Kalkulator naukowy \u2014 rozszerza <code>BasicCalculator</code> o funkcje matematyczne.</p> <p>Dziedziczy wszystkie operacje podstawowe i dodaje: <code>power</code>, <code>sqrt</code>, <code>log</code>, <code>sin</code>, <code>cos</code>.</p> Example <p>calc = ScientificCalculator() calc.calculate(2, 10, \"power\") 1024.0 calc.sqrt(144) 12.0</p> Source code in <code>src/python/calculator.py</code> <pre><code>class ScientificCalculator(BasicCalculator):\n    \"\"\"\n    Kalkulator naukowy \u2014 rozszerza `BasicCalculator` o funkcje matematyczne.\n\n    Dziedziczy wszystkie operacje podstawowe i dodaje:\n    ``power``, ``sqrt``, ``log``, ``sin``, ``cos``.\n\n    Example:\n        &gt;&gt;&gt; calc = ScientificCalculator()\n        &gt;&gt;&gt; calc.calculate(2, 10, \"power\")\n        1024.0\n        &gt;&gt;&gt; calc.sqrt(144)\n        12.0\n    \"\"\"\n\n    def calculate(self, a: float, b: float, operation: str) -&gt; float:\n        \"\"\"\n        Wykonuje operacj\u0119 naukow\u0105 lub deleguje do kalkulatora podstawowego.\n\n        Args:\n            a (float): Pierwsza liczba (lub jedyna dla operacji unarnych).\n            b (float): Druga liczba (lub wyk\u0142adnik/podstawa).\n            operation (str): Jedna z operacji podstawowych lub ``power``, ``log``.\n\n        Returns:\n            float: Wynik operacji.\n\n        Raises:\n            ValueError: Je\u015bli operacja nie jest obs\u0142ugiwana.\n        \"\"\"\n        if operation == \"power\":\n            result = math.pow(a, b)\n            self._record(f\"{a} ^ {b} = {result}\")\n            return result\n        if operation == \"log\":\n            if a &lt;= 0:\n                raise ValueError(\"Logarytm wymaga liczby dodatniej.\")\n            result = math.log(a, b) if b &gt; 0 else math.log(a)\n            self._record(f\"log({a}, {b}) = {result}\")\n            return result\n        return super().calculate(a, b, operation)\n\n    def sqrt(self, a: float) -&gt; float:\n        \"\"\"\n        Oblicza pierwiastek kwadratowy z ``a``.\n\n        Args:\n            a (float): Liczba nieujemna.\n\n        Returns:\n            float: Pierwiastek kwadratowy.\n\n        Raises:\n            ValueError: Je\u015bli ``a &lt; 0``.\n        \"\"\"\n        if a &lt; 0:\n            raise ValueError(\"Pierwiastek z liczby ujemnej jest niezdefiniowany.\")\n        result = math.sqrt(a)\n        self._record(f\"sqrt({a}) = {result}\")\n        return result\n\n    def sin(self, angle_deg: float) -&gt; float:\n        \"\"\"\n        Oblicza sinus k\u0105ta podanego w stopniach.\n\n        Args:\n            angle_deg (float): K\u0105t w stopniach.\n\n        Returns:\n            float: Sinus k\u0105ta.\n        \"\"\"\n        result = math.sin(math.radians(angle_deg))\n        self._record(f\"sin({angle_deg}\u00b0) = {result}\")\n        return result\n\n    def cos(self, angle_deg: float) -&gt; float:\n        \"\"\"\n        Oblicza cosinus k\u0105ta podanego w stopniach.\n\n        Args:\n            angle_deg (float): K\u0105t w stopniach.\n\n        Returns:\n            float: Cosinus k\u0105ta.\n        \"\"\"\n        result = math.cos(math.radians(angle_deg))\n        self._record(f\"cos({angle_deg}\u00b0) = {result}\")\n        return result\n</code></pre> K\u0105ty w stopniach <p>Metody <code>sin()</code> i <code>cos()</code> przyjmuj\u0105 k\u0105t w stopniach, nie radianach. Konwersja do radian\u00f3w odbywa si\u0119 wewn\u0119trznie.</p> <p>               Bases: <code>BasicCalculator</code></p> <p>Kalkulator z pami\u0119ci\u0105 \u2014 rozszerza <code>BasicCalculator</code> o rejestr pami\u0119ci.</p> <p>Umo\u017cliwia zapisywanie i odczytywanie warto\u015bci z pami\u0119ci (wzorowane na klawiszach M+, M-, MR, MC klasycznych kalkulator\u00f3w).</p> <p>Attributes:</p> Name Type Description <code>_memory</code> <code>float</code> <p>Warto\u015b\u0107 aktualnie przechowywana w pami\u0119ci.</p> Example <p>calc = MemoryCalculator() calc.memory_store(42.0) calc.memory_recall() 42.0 calc.memory_add(8.0) calc.memory_recall() 50.0</p> Source code in <code>src/python/calculator.py</code> <pre><code>class MemoryCalculator(BasicCalculator):\n    \"\"\"\n    Kalkulator z pami\u0119ci\u0105 \u2014 rozszerza `BasicCalculator` o rejestr pami\u0119ci.\n\n    Umo\u017cliwia zapisywanie i odczytywanie warto\u015bci z pami\u0119ci (wzorowane\n    na klawiszach M+, M-, MR, MC klasycznych kalkulator\u00f3w).\n\n    Attributes:\n        _memory (float): Warto\u015b\u0107 aktualnie przechowywana w pami\u0119ci.\n\n    Example:\n        &gt;&gt;&gt; calc = MemoryCalculator()\n        &gt;&gt;&gt; calc.memory_store(42.0)\n        &gt;&gt;&gt; calc.memory_recall()\n        42.0\n        &gt;&gt;&gt; calc.memory_add(8.0)\n        &gt;&gt;&gt; calc.memory_recall()\n        50.0\n    \"\"\"\n\n    def __init__(self) -&gt; None:\n        super().__init__()\n        self._memory: float = 0.0\n\n    def memory_store(self, value: float) -&gt; None:\n        \"\"\"\n        Zapisuje warto\u015b\u0107 do pami\u0119ci (MS).\n\n        Args:\n            value (float): Warto\u015b\u0107 do zapisania.\n        \"\"\"\n        self._memory = value\n        self._record(f\"MS: {value}\")\n\n    def memory_recall(self) -&gt; float:\n        \"\"\"\n        Odczytuje warto\u015b\u0107 z pami\u0119ci (MR).\n\n        Returns:\n            float: Warto\u015b\u0107 przechowywana w pami\u0119ci.\n        \"\"\"\n        self._record(f\"MR: {self._memory}\")\n        return self._memory\n\n    def memory_add(self, value: float) -&gt; None:\n        \"\"\"\n        Dodaje warto\u015b\u0107 do pami\u0119ci (M+).\n\n        Args:\n            value (float): Warto\u015b\u0107 do dodania do pami\u0119ci.\n        \"\"\"\n        self._memory += value\n        self._record(f\"M+: {value} \u2192 pami\u0119\u0107 = {self._memory}\")\n\n    def memory_subtract(self, value: float) -&gt; None:\n        \"\"\"\n        Odejmuje warto\u015b\u0107 od pami\u0119ci (M\u2212).\n\n        Args:\n            value (float): Warto\u015b\u0107 do odj\u0119cia od pami\u0119ci.\n        \"\"\"\n        self._memory -= value\n        self._record(f\"M-: {value} \u2192 pami\u0119\u0107 = {self._memory}\")\n\n    def memory_clear(self) -&gt; None:\n        \"\"\"Zeruje pami\u0119\u0107 (MC).\"\"\"\n        self._memory = 0.0\n        self._record(\"MC: pami\u0119\u0107 wyczyszczona\")\n</code></pre>"},{"location":"calculator/#calculator.Calculator.calculate","title":"<code>calculate(a, b, operation)</code>  <code>abstractmethod</code>","text":"<p>Wykonuje operacj\u0119 matematyczn\u0105 na dw\u00f3ch liczbach.</p> <p>Parameters:</p> Name Type Description Default <code>a</code> <code>float</code> <p>Pierwsza liczba (lewy operand).</p> required <code>b</code> <code>float</code> <p>Druga liczba (prawy operand).</p> required <code>operation</code> <code>str</code> <p>Nazwa operacji do wykonania.</p> required <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>Wynik operacji.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>Je\u015bli operacja nie jest obs\u0142ugiwana przez dany kalkulator.</p> Source code in <code>src/python/calculator.py</code> <pre><code>@abstractmethod\ndef calculate(self, a: float, b: float, operation: str) -&gt; float:\n    \"\"\"\n    Wykonuje operacj\u0119 matematyczn\u0105 na dw\u00f3ch liczbach.\n\n    Args:\n        a (float): Pierwsza liczba (lewy operand).\n        b (float): Druga liczba (prawy operand).\n        operation (str): Nazwa operacji do wykonania.\n\n    Returns:\n        float: Wynik operacji.\n\n    Raises:\n        ValueError: Je\u015bli operacja nie jest obs\u0142ugiwana przez dany kalkulator.\n    \"\"\"\n</code></pre>"},{"location":"calculator/#calculator.Calculator.clear_history","title":"<code>clear_history()</code>","text":"<p>Czy\u015bci histori\u0119 operacji.</p> Source code in <code>src/python/calculator.py</code> <pre><code>def clear_history(self) -&gt; None:\n    \"\"\"Czy\u015bci histori\u0119 operacji.\"\"\"\n    self._history.clear()\n</code></pre>"},{"location":"calculator/#calculator.Calculator.get_history","title":"<code>get_history()</code>","text":"<p>Zwraca histori\u0119 wykonanych operacji.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: Kopia listy historii operacji.</p> Source code in <code>src/python/calculator.py</code> <pre><code>def get_history(self) -&gt; list[str]:\n    \"\"\"\n    Zwraca histori\u0119 wykonanych operacji.\n\n    Returns:\n        list[str]: Kopia listy historii operacji.\n    \"\"\"\n    return list(self._history)\n</code></pre>"},{"location":"calculator/#calculator.BasicCalculator.add","title":"<code>add(a, b)</code>","text":"<p>Dodaje dwie liczby. Skr\u00f3t dla <code>calculate(a, b, 'add')</code>.</p> Source code in <code>src/python/calculator.py</code> <pre><code>def add(self, a: float, b: float) -&gt; float:\n    \"\"\"Dodaje dwie liczby. Skr\u00f3t dla ``calculate(a, b, 'add')``.\"\"\"\n    return self.calculate(a, b, \"add\")\n</code></pre>"},{"location":"calculator/#calculator.BasicCalculator.calculate","title":"<code>calculate(a, b, operation)</code>","text":"<p>Wykonuje podstawow\u0105 operacj\u0119 arytmetyczn\u0105.</p> <p>Parameters:</p> Name Type Description Default <code>a</code> <code>float</code> <p>Pierwsza liczba.</p> required <code>b</code> <code>float</code> <p>Druga liczba.</p> required <code>operation</code> <code>str</code> <p>Jedna z: <code>add</code>, <code>subtract</code>, <code>multiply</code>, <code>divide</code>.</p> required <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>Wynik operacji.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>Je\u015bli operacja nie jest obs\u0142ugiwana.</p> <code>ZeroDivisionError</code> <p>Je\u015bli <code>b == 0</code> przy operacji <code>divide</code>.</p> Source code in <code>src/python/calculator.py</code> <pre><code>def calculate(self, a: float, b: float, operation: str) -&gt; float:\n    \"\"\"\n    Wykonuje podstawow\u0105 operacj\u0119 arytmetyczn\u0105.\n\n    Args:\n        a (float): Pierwsza liczba.\n        b (float): Druga liczba.\n        operation (str): Jedna z: ``add``, ``subtract``, ``multiply``, ``divide``.\n\n    Returns:\n        float: Wynik operacji.\n\n    Raises:\n        ValueError: Je\u015bli operacja nie jest obs\u0142ugiwana.\n        ZeroDivisionError: Je\u015bli ``b == 0`` przy operacji ``divide``.\n    \"\"\"\n    if operation == \"add\":\n        result = a + b\n        self._record(f\"{a} + {b} = {result}\")\n    elif operation == \"subtract\":\n        result = a - b\n        self._record(f\"{a} - {b} = {result}\")\n    elif operation == \"multiply\":\n        result = a * b\n        self._record(f\"{a} * {b} = {result}\")\n    elif operation == \"divide\":\n        if b == 0:\n            raise ZeroDivisionError(\"Dzielenie przez zero jest niedozwolone.\")\n        result = a / b\n        self._record(f\"{a} / {b} = {result}\")\n    else:\n        raise ValueError(\n            f\"Nieobs\u0142ugiwana operacja: '{operation}'. \"\n            f\"Dost\u0119pne: {self.SUPPORTED_OPERATIONS}\"\n        )\n    return result\n</code></pre>"},{"location":"calculator/#calculator.BasicCalculator.divide","title":"<code>divide(a, b)</code>","text":"<p>Dzieli <code>a</code> przez <code>b</code>. Skr\u00f3t dla <code>calculate(a, b, 'divide')</code>.</p> <p>Raises:</p> Type Description <code>ZeroDivisionError</code> <p>Je\u015bli <code>b == 0</code>.</p> Source code in <code>src/python/calculator.py</code> <pre><code>def divide(self, a: float, b: float) -&gt; float:\n    \"\"\"\n    Dzieli ``a`` przez ``b``. Skr\u00f3t dla ``calculate(a, b, 'divide')``.\n\n    Raises:\n        ZeroDivisionError: Je\u015bli ``b == 0``.\n    \"\"\"\n    return self.calculate(a, b, \"divide\")\n</code></pre>"},{"location":"calculator/#calculator.BasicCalculator.multiply","title":"<code>multiply(a, b)</code>","text":"<p>Mno\u017cy dwie liczby. Skr\u00f3t dla <code>calculate(a, b, 'multiply')</code>.</p> Source code in <code>src/python/calculator.py</code> <pre><code>def multiply(self, a: float, b: float) -&gt; float:\n    \"\"\"Mno\u017cy dwie liczby. Skr\u00f3t dla ``calculate(a, b, 'multiply')``.\"\"\"\n    return self.calculate(a, b, \"multiply\")\n</code></pre>"},{"location":"calculator/#calculator.BasicCalculator.subtract","title":"<code>subtract(a, b)</code>","text":"<p>Odejmuje <code>b</code> od <code>a</code>. Skr\u00f3t dla <code>calculate(a, b, 'subtract')</code>.</p> Source code in <code>src/python/calculator.py</code> <pre><code>def subtract(self, a: float, b: float) -&gt; float:\n    \"\"\"Odejmuje ``b`` od ``a``. Skr\u00f3t dla ``calculate(a, b, 'subtract')``.\"\"\"\n    return self.calculate(a, b, \"subtract\")\n</code></pre>"},{"location":"calculator/#calculator.ScientificCalculator.calculate","title":"<code>calculate(a, b, operation)</code>","text":"<p>Wykonuje operacj\u0119 naukow\u0105 lub deleguje do kalkulatora podstawowego.</p> <p>Parameters:</p> Name Type Description Default <code>a</code> <code>float</code> <p>Pierwsza liczba (lub jedyna dla operacji unarnych).</p> required <code>b</code> <code>float</code> <p>Druga liczba (lub wyk\u0142adnik/podstawa).</p> required <code>operation</code> <code>str</code> <p>Jedna z operacji podstawowych lub <code>power</code>, <code>log</code>.</p> required <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>Wynik operacji.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>Je\u015bli operacja nie jest obs\u0142ugiwana.</p> Source code in <code>src/python/calculator.py</code> <pre><code>def calculate(self, a: float, b: float, operation: str) -&gt; float:\n    \"\"\"\n    Wykonuje operacj\u0119 naukow\u0105 lub deleguje do kalkulatora podstawowego.\n\n    Args:\n        a (float): Pierwsza liczba (lub jedyna dla operacji unarnych).\n        b (float): Druga liczba (lub wyk\u0142adnik/podstawa).\n        operation (str): Jedna z operacji podstawowych lub ``power``, ``log``.\n\n    Returns:\n        float: Wynik operacji.\n\n    Raises:\n        ValueError: Je\u015bli operacja nie jest obs\u0142ugiwana.\n    \"\"\"\n    if operation == \"power\":\n        result = math.pow(a, b)\n        self._record(f\"{a} ^ {b} = {result}\")\n        return result\n    if operation == \"log\":\n        if a &lt;= 0:\n            raise ValueError(\"Logarytm wymaga liczby dodatniej.\")\n        result = math.log(a, b) if b &gt; 0 else math.log(a)\n        self._record(f\"log({a}, {b}) = {result}\")\n        return result\n    return super().calculate(a, b, operation)\n</code></pre>"},{"location":"calculator/#calculator.ScientificCalculator.cos","title":"<code>cos(angle_deg)</code>","text":"<p>Oblicza cosinus k\u0105ta podanego w stopniach.</p> <p>Parameters:</p> Name Type Description Default <code>angle_deg</code> <code>float</code> <p>K\u0105t w stopniach.</p> required <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>Cosinus k\u0105ta.</p> Source code in <code>src/python/calculator.py</code> <pre><code>def cos(self, angle_deg: float) -&gt; float:\n    \"\"\"\n    Oblicza cosinus k\u0105ta podanego w stopniach.\n\n    Args:\n        angle_deg (float): K\u0105t w stopniach.\n\n    Returns:\n        float: Cosinus k\u0105ta.\n    \"\"\"\n    result = math.cos(math.radians(angle_deg))\n    self._record(f\"cos({angle_deg}\u00b0) = {result}\")\n    return result\n</code></pre>"},{"location":"calculator/#calculator.ScientificCalculator.sin","title":"<code>sin(angle_deg)</code>","text":"<p>Oblicza sinus k\u0105ta podanego w stopniach.</p> <p>Parameters:</p> Name Type Description Default <code>angle_deg</code> <code>float</code> <p>K\u0105t w stopniach.</p> required <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>Sinus k\u0105ta.</p> Source code in <code>src/python/calculator.py</code> <pre><code>def sin(self, angle_deg: float) -&gt; float:\n    \"\"\"\n    Oblicza sinus k\u0105ta podanego w stopniach.\n\n    Args:\n        angle_deg (float): K\u0105t w stopniach.\n\n    Returns:\n        float: Sinus k\u0105ta.\n    \"\"\"\n    result = math.sin(math.radians(angle_deg))\n    self._record(f\"sin({angle_deg}\u00b0) = {result}\")\n    return result\n</code></pre>"},{"location":"calculator/#calculator.ScientificCalculator.sqrt","title":"<code>sqrt(a)</code>","text":"<p>Oblicza pierwiastek kwadratowy z <code>a</code>.</p> <p>Parameters:</p> Name Type Description Default <code>a</code> <code>float</code> <p>Liczba nieujemna.</p> required <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>Pierwiastek kwadratowy.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>Je\u015bli <code>a &lt; 0</code>.</p> Source code in <code>src/python/calculator.py</code> <pre><code>def sqrt(self, a: float) -&gt; float:\n    \"\"\"\n    Oblicza pierwiastek kwadratowy z ``a``.\n\n    Args:\n        a (float): Liczba nieujemna.\n\n    Returns:\n        float: Pierwiastek kwadratowy.\n\n    Raises:\n        ValueError: Je\u015bli ``a &lt; 0``.\n    \"\"\"\n    if a &lt; 0:\n        raise ValueError(\"Pierwiastek z liczby ujemnej jest niezdefiniowany.\")\n    result = math.sqrt(a)\n    self._record(f\"sqrt({a}) = {result}\")\n    return result\n</code></pre>"},{"location":"calculator/#calculator.MemoryCalculator.memory_add","title":"<code>memory_add(value)</code>","text":"<p>Dodaje warto\u015b\u0107 do pami\u0119ci (M+).</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>float</code> <p>Warto\u015b\u0107 do dodania do pami\u0119ci.</p> required Source code in <code>src/python/calculator.py</code> <pre><code>def memory_add(self, value: float) -&gt; None:\n    \"\"\"\n    Dodaje warto\u015b\u0107 do pami\u0119ci (M+).\n\n    Args:\n        value (float): Warto\u015b\u0107 do dodania do pami\u0119ci.\n    \"\"\"\n    self._memory += value\n    self._record(f\"M+: {value} \u2192 pami\u0119\u0107 = {self._memory}\")\n</code></pre>"},{"location":"calculator/#calculator.MemoryCalculator.memory_clear","title":"<code>memory_clear()</code>","text":"<p>Zeruje pami\u0119\u0107 (MC).</p> Source code in <code>src/python/calculator.py</code> <pre><code>def memory_clear(self) -&gt; None:\n    \"\"\"Zeruje pami\u0119\u0107 (MC).\"\"\"\n    self._memory = 0.0\n    self._record(\"MC: pami\u0119\u0107 wyczyszczona\")\n</code></pre>"},{"location":"calculator/#calculator.MemoryCalculator.memory_recall","title":"<code>memory_recall()</code>","text":"<p>Odczytuje warto\u015b\u0107 z pami\u0119ci (MR).</p> <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>Warto\u015b\u0107 przechowywana w pami\u0119ci.</p> Source code in <code>src/python/calculator.py</code> <pre><code>def memory_recall(self) -&gt; float:\n    \"\"\"\n    Odczytuje warto\u015b\u0107 z pami\u0119ci (MR).\n\n    Returns:\n        float: Warto\u015b\u0107 przechowywana w pami\u0119ci.\n    \"\"\"\n    self._record(f\"MR: {self._memory}\")\n    return self._memory\n</code></pre>"},{"location":"calculator/#calculator.MemoryCalculator.memory_store","title":"<code>memory_store(value)</code>","text":"<p>Zapisuje warto\u015b\u0107 do pami\u0119ci (MS).</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>float</code> <p>Warto\u015b\u0107 do zapisania.</p> required Source code in <code>src/python/calculator.py</code> <pre><code>def memory_store(self, value: float) -&gt; None:\n    \"\"\"\n    Zapisuje warto\u015b\u0107 do pami\u0119ci (MS).\n\n    Args:\n        value (float): Warto\u015b\u0107 do zapisania.\n    \"\"\"\n    self._memory = value\n    self._record(f\"MS: {value}\")\n</code></pre>"},{"location":"calculator/#calculator.MemoryCalculator.memory_subtract","title":"<code>memory_subtract(value)</code>","text":"<p>Odejmuje warto\u015b\u0107 od pami\u0119ci (M\u2212).</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>float</code> <p>Warto\u015b\u0107 do odj\u0119cia od pami\u0119ci.</p> required Source code in <code>src/python/calculator.py</code> <pre><code>def memory_subtract(self, value: float) -&gt; None:\n    \"\"\"\n    Odejmuje warto\u015b\u0107 od pami\u0119ci (M\u2212).\n\n    Args:\n        value (float): Warto\u015b\u0107 do odj\u0119cia od pami\u0119ci.\n    \"\"\"\n    self._memory -= value\n    self._record(f\"M-: {value} \u2192 pami\u0119\u0107 = {self._memory}\")\n</code></pre>"},{"location":"python/","title":"Dokumentacja API \u2014 Wzorzec Strategii","text":"<p>Automatycznie wygenerowana dokumentacja modu\u0142u <code>pattern.py</code>, implementuj\u0105cego wzorzec projektowy Strategia (Strategy Pattern).</p>"},{"location":"python/#przykad-uzycia","title":"Przyk\u0142ad u\u017cycia","text":"PythonScala <pre><code>from pattern import CreditCardPayment, PayPalPayment\n\nstrategy = CreditCardPayment()       # (1)\nprint(strategy.pay(99.99))           # (2)\n\nstrategy = PayPalPayment()           # (3)\nprint(strategy.pay(49.99))\n</code></pre> <ol> <li>Tworzymy konkretn\u0105 strategi\u0119 \u2014 mo\u017cemy j\u0105 podmieni\u0107 w dowolnym momencie</li> <li>Wypisuje: <code>Zap\u0142acono 99.99 PLN przy u\u017cyciu karty kredytowej.</code></li> <li>Podmiana strategii \u2014 kod klienta si\u0119 nie zmienia, zmienia si\u0119 tylko obiekt</li> </ol> <pre><code>import payment._\n\nval strategy: PaymentStrategy =      // (1)\n  new CreditCardPayment(\"4111111111111111\")\nprintln(strategy.pay(99.99))         // (2)\n\nval strategy2: PaymentStrategy =\n  new PayPalPayment(\"user@example.com\")\nprintln(strategy2.pay(49.99))\n</code></pre> <ol> <li>Typ zmiennej to <code>PaymentStrategy</code> (trait), nie konkretna klasa \u2014 to sedno wzorca</li> <li>Wypisuje: <code>Zap\u0142acono 99.99 PLN kart\u0105 kredytow\u0105 (nr: ****1111).</code></li> </ol>"},{"location":"python/#dokumentacja-klas","title":"Dokumentacja klas","text":"Klasa abstrakcyjna \u2014 nie mo\u017cna instancjonowa\u0107 bezpo\u015brednio <p><code>PaymentStrategy</code> jest klas\u0105 bazow\u0105 definiuj\u0105c\u0105 kontrakt. Zawsze u\u017cywaj jednej z klas pochodnych: <code>CreditCardPayment</code> lub <code>PayPalPayment</code>.</p> Szczeg\u00f3\u0142y implementacyjne \u2014 CreditCardPayment <p>Metoda <code>pay()</code> nie wykonuje rzeczywistej transakcji \u2014 symuluje logik\u0119 autoryzacji kartowej. W \u015brodowisku produkcyjnym nale\u017cy zast\u0105pi\u0107 j\u0105 integracj\u0105 z bramk\u0105 p\u0142atnicz\u0105 (np. Stripe, Adyen).</p> Szczeg\u00f3\u0142y implementacyjne \u2014 PayPalPayment <p>Metoda <code>pay()</code> symuluje transfer PayPal. W \u015brodowisku produkcyjnym wymaga integracji z PayPal REST API i obs\u0142ugi token\u00f3w OAuth 2.0.</p>"},{"location":"python/#pattern.PaymentStrategy","title":"<code>pattern.PaymentStrategy</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Klasa abstrakcyjna (interfejs) dla strategii p\u0142atno\u015bci.</p> <p>Definiuje wsp\u00f3lny kontrakt dla wszystkich metod p\u0142atno\u015bci obs\u0142ugiwanych  w systemie. Implementuje wzorzec projektowy Strategy.</p> Source code in <code>src/python/pattern.py</code> <pre><code>class PaymentStrategy(ABC):\n    \"\"\"\n    Klasa abstrakcyjna (interfejs) dla strategii p\u0142atno\u015bci.\n\n    Definiuje wsp\u00f3lny kontrakt dla wszystkich metod p\u0142atno\u015bci obs\u0142ugiwanych \n    w systemie. Implementuje wzorzec projektowy Strategy.\n    \"\"\"\n\n    @abstractmethod\n    def pay(self, amount: float) -&gt; str:\n        \"\"\"\n        Przetwarza p\u0142atno\u015b\u0107 na podan\u0105 kwot\u0119.\n\n        Args:\n            amount (float): Kwota do zap\u0142aty.\n\n        Returns:\n            str: Komunikat potwierdzaj\u0105cy status p\u0142atno\u015bci.\n        \"\"\"\n        pass\n</code></pre>"},{"location":"python/#pattern.PaymentStrategy.pay","title":"<code>pay(amount)</code>  <code>abstractmethod</code>","text":"<p>Przetwarza p\u0142atno\u015b\u0107 na podan\u0105 kwot\u0119.</p> <p>Parameters:</p> Name Type Description Default <code>amount</code> <code>float</code> <p>Kwota do zap\u0142aty.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Komunikat potwierdzaj\u0105cy status p\u0142atno\u015bci.</p> Source code in <code>src/python/pattern.py</code> <pre><code>@abstractmethod\ndef pay(self, amount: float) -&gt; str:\n    \"\"\"\n    Przetwarza p\u0142atno\u015b\u0107 na podan\u0105 kwot\u0119.\n\n    Args:\n        amount (float): Kwota do zap\u0142aty.\n\n    Returns:\n        str: Komunikat potwierdzaj\u0105cy status p\u0142atno\u015bci.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"python/#pattern.CreditCardPayment","title":"<code>pattern.CreditCardPayment</code>","text":"<p>               Bases: <code>PaymentStrategy</code></p> <p>Strategia p\u0142atno\u015bci kart\u0105 kredytow\u0105.</p> <p>Dziedziczy po klasie <code>PaymentStrategy</code>. Odpowiada za symulacj\u0119 logiki  autoryzacji i pobierania \u015brodk\u00f3w z karty kredytowej.</p> Source code in <code>src/python/pattern.py</code> <pre><code>class CreditCardPayment(PaymentStrategy):\n    \"\"\"\n    Strategia p\u0142atno\u015bci kart\u0105 kredytow\u0105.\n\n    Dziedziczy po klasie `PaymentStrategy`. Odpowiada za symulacj\u0119 logiki \n    autoryzacji i pobierania \u015brodk\u00f3w z karty kredytowej.\n    \"\"\"\n\n    def pay(self, amount: float) -&gt; str:\n        \"\"\"\n        Realizuje p\u0142atno\u015b\u0107 przy u\u017cyciu karty kredytowej.\n\n        Args:\n            amount (float): Kwota do zap\u0142aty.\n\n        Returns:\n            str: Szczeg\u00f3\u0142y transakcji kart\u0105 kredytow\u0105.\n        \"\"\"\n        return f\"Zap\u0142acono {amount} PLN przy u\u017cyciu karty kredytowej.\"\n</code></pre>"},{"location":"python/#pattern.CreditCardPayment.pay","title":"<code>pay(amount)</code>","text":"<p>Realizuje p\u0142atno\u015b\u0107 przy u\u017cyciu karty kredytowej.</p> <p>Parameters:</p> Name Type Description Default <code>amount</code> <code>float</code> <p>Kwota do zap\u0142aty.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Szczeg\u00f3\u0142y transakcji kart\u0105 kredytow\u0105.</p> Source code in <code>src/python/pattern.py</code> <pre><code>def pay(self, amount: float) -&gt; str:\n    \"\"\"\n    Realizuje p\u0142atno\u015b\u0107 przy u\u017cyciu karty kredytowej.\n\n    Args:\n        amount (float): Kwota do zap\u0142aty.\n\n    Returns:\n        str: Szczeg\u00f3\u0142y transakcji kart\u0105 kredytow\u0105.\n    \"\"\"\n    return f\"Zap\u0142acono {amount} PLN przy u\u017cyciu karty kredytowej.\"\n</code></pre>"},{"location":"python/#pattern.PayPalPayment","title":"<code>pattern.PayPalPayment</code>","text":"<p>               Bases: <code>PaymentStrategy</code></p> <p>Strategia p\u0142atno\u015bci za po\u015brednictwem systemu PayPal.</p> <p>Dziedziczy po klasie <code>PaymentStrategy</code>. Odpowiada za symulacj\u0119 logiki  logowania do konta PayPal i transferu \u015brodk\u00f3w.</p> Source code in <code>src/python/pattern.py</code> <pre><code>class PayPalPayment(PaymentStrategy):\n    \"\"\"\n    Strategia p\u0142atno\u015bci za po\u015brednictwem systemu PayPal.\n\n    Dziedziczy po klasie `PaymentStrategy`. Odpowiada za symulacj\u0119 logiki \n    logowania do konta PayPal i transferu \u015brodk\u00f3w.\n    \"\"\"\n\n    def pay(self, amount: float) -&gt; str:\n        \"\"\"\n        Realizuje p\u0142atno\u015b\u0107 przy u\u017cyciu konta PayPal.\n\n        Args:\n            amount (float): Kwota do zap\u0142aty.\n\n        Returns:\n            str: Szczeg\u00f3\u0142y transakcji w systemie PayPal.\n        \"\"\"\n        return f\"Zap\u0142acono {amount} PLN przy u\u017cyciu systemu PayPal.\"\n</code></pre>"},{"location":"python/#pattern.PayPalPayment.pay","title":"<code>pay(amount)</code>","text":"<p>Realizuje p\u0142atno\u015b\u0107 przy u\u017cyciu konta PayPal.</p> <p>Parameters:</p> Name Type Description Default <code>amount</code> <code>float</code> <p>Kwota do zap\u0142aty.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Szczeg\u00f3\u0142y transakcji w systemie PayPal.</p> Source code in <code>src/python/pattern.py</code> <pre><code>def pay(self, amount: float) -&gt; str:\n    \"\"\"\n    Realizuje p\u0142atno\u015b\u0107 przy u\u017cyciu konta PayPal.\n\n    Args:\n        amount (float): Kwota do zap\u0142aty.\n\n    Returns:\n        str: Szczeg\u00f3\u0142y transakcji w systemie PayPal.\n    \"\"\"\n    return f\"Zap\u0142acono {amount} PLN przy u\u017cyciu systemu PayPal.\"\n</code></pre>"},{"location":"scala/","title":"Dokumentacja API Scali","text":"<p>Poni\u017cej znajduje si\u0119 automatycznie wygenerowana dokumentacja Scaladoc dla modu\u0142u implementuj\u0105cego wzorzec projektowy Strategia (pakiet <code>payment</code>).</p> <p>Generowanie dokumentacji</p> <p>Dokumentacja Scaladoc jest generowana automatycznie przez <code>sbt doc</code> w potoku CI/CD. Lokalne uruchomienie: <code>sbt doc</code> \u2192 pliki trafiaj\u0105 do <code>target/scala-2.13/api/</code>.</p>"},{"location":"diagrams/python_calculator/","title":"calculator","text":""},{"location":"diagrams/python_pattern/","title":"pattern","text":""}]}