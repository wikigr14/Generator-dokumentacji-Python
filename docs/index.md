# Witaj w Projekcie 18

To jest główna strona naszej automatycznie generowanej i wersjonowanej dokumentacji.

Skorzystaj z zakładek w menu powyżej (lub z boku), aby przeglądać dokumentację wygenerowaną prosto z kodu źródłowego projektów napisanych w językach Python oraz Scala.

## Szybki start

- Moduł Pythona został wygenerowany za pomocą wtyczki `mkdocstrings`.
- Moduł Scali to w pełni zintegrowany natywny format `Scaladoc`.

### Test Zakładek i Ostrzeżeń

??? warning "Ważne ostrzeżenie (kliknij, aby rozwinąć)"
    Jeśli widzisz ten tekst po kliknięciu, rozwijane panele (Admonitions) działają poprawnie!

=== "Kod w Pythonie"

    ```python
    def test_funkcji():
        print("Zakładki działają!")
    ```

=== "Kod w Scali"

    ```scala
    def testFunkcji(): Unit = {
      println("Zakładki działają!")
    }
    ```
